
from setuptools import setup, find_packages

setup(
    name='beam_accounting',
    version='0.0.1',
    description='BEAM modeling with ChatGPT and emissions calculator',
    author='Vivek Singh',
    author_email='vivek@example.com',
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[]
)
